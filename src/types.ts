export interface ItemType {
  id: string;
  name: string;
  // category: string;
  // locationHome: string;
  // locationStore: string;
  qtyHome: number;
  qtyList: number;
  // shelfLife: number;
}
